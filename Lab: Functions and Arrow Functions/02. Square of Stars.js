function squareOfStars(num){
    let stars ='* '.repeat(num);
    for (let row=0; row< num; row++){
      for(let col=0; col <num; col++){
        //stars+='*';
      }
      console.log(stars)
    }
  }